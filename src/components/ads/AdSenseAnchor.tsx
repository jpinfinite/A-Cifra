'use client'

/**
 * Anúncio Âncora (Sticky Bottom) - Excelente para mobile (RPM $10-15)
 * Fica fixo na parte inferior da tela
 * 
 * NOTA: O enable_page_level_ads já está configurado no layout.tsx
 * Este componente não precisa fazer nada, o AdSense gerencia automaticamente
 */
export function AdSenseAnchor() {
  return null // Anúncio âncora é gerenciado automaticamente pelo AdSense via layout.tsx
}
